# Extract canonical parameter truth from simulated data

Extract canonical parameter truth from simulated data

## Usage

``` r
extract_parameter_truth(simulation)
```

## Arguments

- simulation:

  Simulation object containing \`truth\`, \`parameters\`, or a \`truth\`
  attribute.

## Value

Data frame with \`parameter\` and \`truth\`.
