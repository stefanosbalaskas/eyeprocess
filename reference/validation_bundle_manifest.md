# Create a machine-readable validation manifest

Create a machine-readable validation manifest

## Usage

``` r
validation_bundle_manifest(x)
```

## Arguments

- x:

  Object to process, inspect, compare, or plot.

## Value

A data frame containing a machine-readable validation manifest. Rows
represent the analysis units and columns contain the identifiers,
estimates, or diagnostics defined by the function.
