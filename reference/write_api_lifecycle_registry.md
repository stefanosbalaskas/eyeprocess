# Write API lifecycle registry to CSV

Write API lifecycle registry to CSV

## Usage

``` r
write_api_lifecycle_registry(registry, path)
```

## Arguments

- registry:

  Lifecycle registry.

- path:

  Output path.

## Value

An R object containing aPI lifecycle registry to CSV. The concrete class
and structure follow the selected method, engine, or input object and
are preserved as documented by that workflow.
