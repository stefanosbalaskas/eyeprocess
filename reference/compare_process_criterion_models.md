# Compare process external-validity models

Compare process external-validity models

## Usage

``` r
compare_process_criterion_models(x)
```

## Arguments

- x:

  Object to process, inspect, compare, or plot.

## Value

A data frame containing process external-validity models. Rows represent
the analysis units and columns contain the identifiers, estimates, or
diagnostics defined by the function.
