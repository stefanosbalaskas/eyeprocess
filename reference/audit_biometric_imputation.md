# Alias emphasizing sensitivity rather than automatic replacement

Alias emphasizing sensitivity rather than automatic replacement

## Usage

``` r
audit_biometric_imputation(...)
```

## Arguments

- ...:

  Additional arguments passed to the underlying method or helper.

## Value

An R object containing alias emphasizing sensitivity rather than
automatic replacement. The concrete class and structure follow the
selected method, engine, or input object and are preserved as documented
by that workflow.
