# Process-measure guardrail table

Process-measure guardrail table

## Usage

``` r
process_measure_guardrails(registry = process_measure_registry())
```

## Arguments

- registry:

  Registry.

## Value

An R object containing process-measure guardrail table. The concrete
class and structure follow the selected method, engine, or input object
and are preserved as documented by that workflow.
