# Registry of specialized external IRT engines

Registry of specialized external IRT engines

## Usage

``` r
eyeprocess_irt_engine_registry()
```

## Value

An object of class "eye_irt_engine_registry", "data.frame", stored as a
data frame, containing registry of specialized external IRT engines and
associated metadata needed to interpret the result.
