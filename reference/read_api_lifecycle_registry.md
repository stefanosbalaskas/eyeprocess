# Read API lifecycle registry from CSV

Read API lifecycle registry from CSV

## Usage

``` r
read_api_lifecycle_registry(path)
```

## Arguments

- path:

  CSV path.

## Value

An R object containing aPI lifecycle registry from CSV. The concrete
class and structure follow the selected method, engine, or input object
and are preserved as documented by that workflow.
