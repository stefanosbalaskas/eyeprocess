# Hash an R object reproducibly within an R serialization version

Hash an R object reproducibly within an R serialization version

## Usage

``` r
object_hash(x)
```

## Arguments

- x:

  R object.

## Value

An R object containing hash an R object reproducibly within an R
serialization version. The concrete class and structure follow the
selected method, engine, or input object and are preserved as documented
by that workflow.
