# Extract a pipeline result by step name

Extract a pipeline result by step name

## Usage

``` r
pipeline_result(x, step)
```

## Arguments

- x:

  Pipeline run.

- step:

  Step name.

## Value

An R object containing a pipeline result by step name. The concrete
class and structure follow the selected method, engine, or input object
and are preserved as documented by that workflow.
