# Write a decision manifest

Write a decision manifest

## Usage

``` r
write_decision_manifest(x, path, format = c("rds", "dput", "json"))
```

## Arguments

- x:

  Manifest.

- path:

  Output path.

- format:

  \`rds\`, \`dput\`, or \`json\`.
