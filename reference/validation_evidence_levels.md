# Detailed validation evidence levels

Returns the fine-grained evidence ladder used by the 0.7 validation
programme. This is deliberately orthogonal to the existing public
support levels (declared / fixture-tested / empirically-validated), so
existing compatibility claims remain stable.

## Usage

``` r
validation_evidence_levels()
```

## Value

A data frame ordered from weakest to strongest evidence.
