# Extract multiblock person coordinates

Extract multiblock person coordinates

## Usage

``` r
multiblock_person_coordinates(x)
```

## Arguments

- x:

  Object to process, inspect, compare, or plot.

## Value

An R object containing multiblock person coordinates. The concrete class
and structure follow the selected method, engine, or input object and
are preserved as documented by that workflow.
