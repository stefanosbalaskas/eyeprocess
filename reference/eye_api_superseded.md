# Return superseded/deprecated compatibility interfaces

Return superseded/deprecated compatibility interfaces

## Usage

``` r
eye_api_superseded(registry = eye_api_lifecycle())
```

## Arguments

- registry:

  Lifecycle registry.

## Value

A tabular R object containing return superseded/deprecated compatibility
interfaces; rows represent analysis units and columns contain the
returned quantities.
