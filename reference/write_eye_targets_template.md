# Write an explicit \`\_targets.R\` template from a governed pipeline

Write an explicit \`\_targets.R\` template from a governed pipeline

## Usage

``` r
write_eye_targets_template(x, path = "_targets.R")
```

## Arguments

- x:

  Pipeline.

- path:

  Output path.

## Value

An R object containing an explicit \`\_targets.R\` template from a
governed pipeline. The concrete class and structure follow the selected
method, engine, or input object and are preserved as documented by that
workflow.
