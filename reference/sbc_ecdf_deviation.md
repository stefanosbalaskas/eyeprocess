# ECDF deviation summary for SBC ranks

ECDF deviation summary for SBC ranks

## Usage

``` r
sbc_ecdf_deviation(x, n_draws = NULL)
```

## Arguments

- x:

  SBC diagnostics or ranks.

- n_draws:

  Required if x is ranks.

## Value

An R object containing eCDF deviation summary for SBC ranks. The
concrete class and structure follow the selected method, engine, or
input object and are preserved as documented by that workflow.
