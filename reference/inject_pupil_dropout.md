# Inject pupil dropout

Inject pupil dropout

## Usage

``` r
inject_pupil_dropout(data, pupil = "pupil", proportion, seed = 1L)
```

## Arguments

- data:

  Data.

- pupil:

  Pupil column.

- proportion:

  Dropout proportion.

- seed:

  Seed.

## Value

An R object containing inject pupil dropout. The concrete class and
structure follow the selected method, engine, or input object and are
preserved as documented by that workflow.
