# Return a one-measure process card

Return a one-measure process card

## Usage

``` r
process_measure_card(name, registry = process_measure_registry())
```

## Arguments

- name:

  Measure name.

- registry:

  Registry.
