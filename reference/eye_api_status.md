# Lookup lifecycle status for one or more APIs

Lookup lifecycle status for one or more APIs

## Usage

``` r
eye_api_status(name, registry = eye_api_lifecycle())
```

## Arguments

- name:

  API names.

- registry:

  Lifecycle registry.
