# Create an explicit pre-flight exclusion/review manifest

The manifest records recommendations only. It does not remove
observations.

## Usage

``` r
preflight_exclusion_manifest(x)
```

## Arguments

- x:

  Object to process, inspect, compare, or plot.
