# Compare two reproducibility fingerprints

Compare two reproducibility fingerprints

## Usage

``` r
compare_reproducibility_fingerprints(old, new)
```

## Arguments

- old, new:

  Fingerprints.

## Value

A named list with components "detail", "identical", "old_hash",
"new_hash", containing two reproducibility fingerprints and associated
metadata or diagnostics.
