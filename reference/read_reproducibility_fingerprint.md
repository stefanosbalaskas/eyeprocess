# Read a reproducibility fingerprint

Read a reproducibility fingerprint

## Usage

``` r
read_reproducibility_fingerprint(path, format = NULL)
```

## Arguments

- path:

  Input path.

- format:

  Optional format.
